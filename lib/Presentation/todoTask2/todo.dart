import 'package:flutter/material.dart';
import 'package:task_1/Presentation/todoTask2/pages/home.dart';

class ToDo extends StatelessWidget {
  const ToDo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Home(),
    );
  }
}
